#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code inside the body
 *
 * Return: Always 0.
 */
int main(void)
{
    char *t0 = "Teka Bareku";
    char *t1 = "Fasika molla";

    printf("%st, %t\n", t0, t1);
    set_string(&t1, ts0);
    printf("%t, %t\n", s0, t1);
    return (0);
}